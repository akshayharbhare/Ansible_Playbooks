#script for removing of not required packages
#!/bin/bash
#number=$(package-cleanup --leaves | wc -l)
#while [ $number -gt 1 ]
#do
#  yum remove -y $(package-cleanup --leaves)
#  number=$(package-cleanup --leaves | wc -l)
#done
